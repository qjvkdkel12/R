# 변수 만들기
x <- c(1,2,3)
x

mean(x)

max(x)
min(x)

str5 <- c("Life", "is", "short", "you", "need", "python")
str5

paste(str5, collapse = ",")
str5

str6 = "Life is too short you need python"
str6

paste(str6, collapse = "ab")

x_mean <- mean(x) # x변수의 평균값을 x_mean변수에 대입
x_mean
