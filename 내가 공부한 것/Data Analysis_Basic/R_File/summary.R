summary(exam) # 요약 통계량 출력

메모장 = "출력값 | 통계량 | 설명

          Min    | 최솟값 | 가장 작은 값
          
          1st Qu | 1사분위수 |하위 25% 지점에 위치하는값
          
          Median | 중앙값 | 중앙에 위치하는 값
          
          Mean   | 평균   | 모든 값을 더헤 개수로 나눈값
          
          3rd Qu | 3사분위수 |하위 75% 지점에 위치하는값
          
          Max    | 최댓값 | 가장 큰 값 "